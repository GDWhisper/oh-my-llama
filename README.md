# Llama Launcher

一个用于启动和管理 `llama-server` 的 Tauri 桌面启动器。

## 开发

```
npm install
npm run tauri dev
```

## 预览端口

当前开发预览统一使用 `6060`。

## 验证

- `npm run build`
- `npm run check`
- `cargo test --lib --manifest-path src-tauri/Cargo.toml`
