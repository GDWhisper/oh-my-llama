# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Agent / tool local state (not project source)
/.codegraph/
/.workbuddy/

# Foreign AI-tool scaffolding (not project source)
.codex/
.qoder/
.codebuddy/
.claude/
.mcp.json
.omp/
.pi/
.box-agent/
openspec/
.dev_docs/changes/

# Local scratch (build/test debris) — never commit
tmp/

# Box-Agent session working dirs (local scratch, not project source) — never commit
/20[0-9][0-9]-[0-9][0-9]-[0-9][0-9]-*/

# Promotion / video production material (local, not project source) — never commit
/promo-video-kit/
/promo-material/

# Whole .codebuddy/ is foreign AI-tool scaffolding (not project source) — fully ignored, do not track or push.
